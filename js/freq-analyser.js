var canvas;

function setup() {
  // canvas = createCanvas(500, 15);
  // canvas.parent("freq-analyser");
  //
  // noFill();
  // mic = new p5.AudioIn();
  // mic.start();
  // fft = new p5.FFT();
  // fft.setInput(mic);
}

function draw() {
  // background(246);
  // var spectrum = fft.analyze();
  //
  // beginShape();
  // for (var i = 0; i < spectrum.length; i++) {
  //   vertex(i, map(spectrum[i], 0, 255, height, 0) );
  // }
  // endShape();
}
